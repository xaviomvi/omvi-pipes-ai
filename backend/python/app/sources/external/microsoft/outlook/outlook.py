    # Auto-generated Microsoft Outlook API client.
    # ruff: noqa: BLE001, D417, E501, ANN003, PGH003, PLR0912, C901, PLR0913, G004, TRY400, TRY003, EM101, D100, INP001

import keyword as _kw
import logging
import re
from collections.abc import Mapping

from app.sources.client.microsoft.microsoft import MSGraphClient, MSGraphResponse

# Set up logger
logger = logging.getLogger(__name__)

class OutlookDataSource:
    """Auto-generated Microsoft Outlook Mail and Calendar API client wrapper.

    - Uses Microsoft Graph SDK client internally
    - Snake_case method names for all Microsoft Outlook Mail and Calendar API operations
    - Standardized MSGraphResponse format for all responses
    - No direct HTTP calls - all requests go through Graph SDK
    """

    def __init__(self, client: MSGraphClient) -> None:
        """Initialize with Microsoft Graph SDK client."""
        self.client = client.get_client()
        if not hasattr(self.client, "me"):
            raise ValueError("Client must be a Microsoft Graph SDK client")
        logger.info("Microsoft Outlook Mail and Calendar API client initialized successfully")

    def _sanitize_py_name(self, name: str) -> str:
        """Runtime-safe version of sanitize_py_name for mirroring param keys.

        Turns '$select' -> 'dollar_select', 'message-id' -> 'message_id', etc.
        """
        n = name.replace("$", "dollar_").replace("@", "at_").replace(".", "_")
        n = re.sub(r"[^0-9a-zA-Z_]", "_", n)
        if n and n[0].isdigit():
            n = f"_{n}"
        if _kw.iskeyword(n):
            n += "_"
        if n.startswith("__"):
            n = f"_{n}"
        return n

    def _handle_response(self, response: dict[str, str | int | bool | list[object]]) -> MSGraphResponse:
        """Handle Microsoft Graph API response."""
        try:
            if response is None:
                    return MSGraphResponse(success=False, error="Empty response from Microsoft Graph")

            success = True
            error_msg = None

            # Handle error responses
            if hasattr(response, "error"):
                success = False
                error_msg = str(response.error) #type:ignore
            elif isinstance(response, dict) and "error" in response:
                success = False
                error_msg = str(response["error"])
            elif hasattr(response, "code") and hasattr(response, "message"):
                success = False
                error_msg = f"{response.code}: {response.message}" #type:ignore

            return MSGraphResponse(
                success=success,
                data=response,
                error=error_msg,
            )
        except Exception as e:
            logger.error(f"Error handling Microsoft Outlook Mail and Calendar API response: {e}")
            return MSGraphResponse(success=False, error=str(e))

    def get_data_source(self) -> "OutlookDataSource":
        """Get the underlying Microsoft Outlook Mail and Calendar API client."""
        return self


    def me_create_calendar_groups(self, *, request_body: Mapping[str, object] | None = None, **kwargs) -> MSGraphResponse:
        """Create CalendarGroup.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups (POST).

        Args:
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        # No parameters
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups()  # type:ignore
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_create_calendars(self, *, request_body: Mapping[str, object] | None = None, **kwargs) -> MSGraphResponse:
        """Create calendar.

        Microsoft Outlook Mail and Calendar API method: /me/calendars (POST).

        Args:
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        # No parameters
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars()  # type:ignore
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_create_events(self, *, request_body: Mapping[str, object] | None = None, **kwargs) -> MSGraphResponse:
        """Create event.

        Microsoft Outlook Mail and Calendar API method: /me/events (POST).

        Args:
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        # No parameters
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events()  # type:ignore
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_create_mail_folders(self, *, request_body: Mapping[str, object] | None = None, **kwargs) -> MSGraphResponse:
        """Create MailFolder.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders (POST).

        Args:
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        # No parameters
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_create_messages(self, *, request_body: Mapping[str, object] | None = None, **kwargs) -> MSGraphResponse:
        """Create open extension.

        Microsoft Outlook Mail and Calendar API method: /me/messages (POST).

        Args:
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        # No parameters
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages()  # type:ignore
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_delete_calendar_groups(self,
        *,
        calendar_group_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete calendarGroup.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id} (DELETE).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_delete_calendars(self,
        *,
        calendar_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property calendars for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id} (DELETE).

        Args:
            calendar_id (required): The unique identifier of calendar
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_delete_events(self,
        *,
        event_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete event.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id} (DELETE).

        Args:
            event_id (required): The unique identifier of event
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_delete_mail_folders(self,
        *,
        mail_folder_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete mailFolder.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id} (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_delete_messages(self,
        *,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete message.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id} (DELETE).

        Args:
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_delete_messages_content(self,
        *,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete message.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/$value (DELETE).

        Args:
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).value().delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_get_calendar(self,
        *,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendar.

        Microsoft Outlook Mail and Calendar API method: /me/calendar (GET).

        Args:
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().get(params=params)  # type:ignore
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_get_calendar_groups(self,
        *,
        calendar_group_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendarGroup.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id} (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_get_calendars(self,
        *,
        calendar_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendars from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id} (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_get_events(self,
        *,
        event_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get event.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id} (GET).

        Args:
            event_id (required): The unique identifier of event
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_get_mail_folders(self,
        *,
        mail_folder_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get mailFolder.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id} (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_get_messages(self,
        *,
        message_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get singleValueLegacyExtendedProperty.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id} (GET).

        Args:
            message_id (required): The unique identifier of message
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_get_messages_content(self,
        *,
        message_id: str,
        include_hidden_messages: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List messages.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/$value (GET).

        Args:
            message_id (required): The unique identifier of message
            include_hidden_messages (optional): Include Hidden Messages

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if include_hidden_messages is not None:
            params["includeHiddenMessages"] = include_hidden_messages
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).value().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_list_calendar_groups(self,
        *,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List calendarGroups.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups (GET).

        Args:
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_list_calendar_view(self,
        *,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List calendarView.

        Microsoft Outlook Mail and Calendar API method: /me/calendarView (GET).

        Args:
            start_date_time (required): The start date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T19:00:00-08:00
            end_date_time (required): The end date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_view()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_list_calendars(self,
        *,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List calendars.

        Microsoft Outlook Mail and Calendar API method: /me/calendars (GET).

        Args:
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().get(params=params)  # type:ignore
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_list_events(self,
        *,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List events.

        Microsoft Outlook Mail and Calendar API method: /me/events (GET).

        Args:
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().get(params=params)  # type:ignore
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_list_mail_folders(self,
        *,
        include_hidden_folders: str | None = None,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List mailFolders.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders (GET).

        Args:
            include_hidden_folders (optional): Include Hidden Folders
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if include_hidden_folders is not None:
            params["includeHiddenFolders"] = include_hidden_folders
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_list_messages(self,
        *,
        include_hidden_messages: str | None = None,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List messages.

        Microsoft Outlook Mail and Calendar API method: /me/messages (GET).

        Args:
            include_hidden_messages (optional): Include Hidden Messages
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if include_hidden_messages is not None:
            params["includeHiddenMessages"] = include_hidden_messages
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_update_calendar(self, *, request_body: Mapping[str, object] | None = None, **kwargs) -> MSGraphResponse:
        """Update calendar.

        Microsoft Outlook Mail and Calendar API method: /me/calendar (PATCH).

        Args:
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        # No parameters
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar()  # type:ignore
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_update_calendar_groups(self,
        *,
        calendar_group_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update calendargroup.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id} (PATCH).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_update_calendars(self,
        *,
        calendar_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property calendars in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id} (PATCH).

        Args:
            calendar_id (required): The unique identifier of calendar
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_update_events(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update event.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id} (PATCH).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_update_mail_folders(self,
        *,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update mailSearchFolder.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id} (PATCH).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_update_messages(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update message.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id} (PATCH).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_update_messages_content(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update message.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/$value (PUT).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).value().put(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_create_calendar_permissions(self, *, request_body: Mapping[str, object] | None = None, **kwargs) -> MSGraphResponse:
        """Create calendarPermission.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/calendarPermissions (POST).

        Args:
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        # No parameters
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar()  # type:ignore
            .calendar_permissions().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_create_events(self, *, request_body: Mapping[str, object] | None = None, **kwargs) -> MSGraphResponse:
        """Create new navigation property to events for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events (POST).

        Args:
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        # No parameters
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_delete_calendar_permissions(self,
        *,
        calendar_permission_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property calendarPermissions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/calendarPermissions/{calendarPermission-id} (DELETE).

        Args:
            calendar_permission_id (required): The unique identifier of calendarPermission
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_permission_id is not None:
            params["calendarPermission-id"] = calendar_permission_id
            params["calendarPermission_id"] = calendar_permission_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar()  # type:ignore
            .calendar_permissions().by_id(params.get("calendarPermission_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_delete_events(self,
        *,
        event_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property events for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id} (DELETE).

        Args:
            event_id (required): The unique identifier of event
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_get_calendar_permissions(self,
        *,
        calendar_permission_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendarPermissions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/calendarPermissions/{calendarPermission-id} (GET).

        Args:
            calendar_permission_id (required): The unique identifier of calendarPermission
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_permission_id is not None:
            params["calendarPermission-id"] = calendar_permission_id
            params["calendarPermission_id"] = calendar_permission_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar()  # type:ignore
            .calendar_permissions().by_id(params.get("calendarPermission_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_get_events(self,
        *,
        event_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get events from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id} (GET).

        Args:
            event_id (required): The unique identifier of event
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_list_calendar_permissions(self,
        *,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendarPermissions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/calendarPermissions (GET).

        Args:
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar()  # type:ignore
            .calendar_permissions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_list_calendar_view(self,
        *,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List calendarView.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/calendarView (GET).

        Args:
            start_date_time (required): The start date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T19:00:00-08:00
            end_date_time (required): The end date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().calendar_view()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_list_events(self,
        *,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List events.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events (GET).

        Args:
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_update_calendar_permissions(self,
        *,
        calendar_permission_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property calendarPermissions in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/calendarPermissions/{calendarPermission-id} (PATCH).

        Args:
            calendar_permission_id (required): The unique identifier of calendarPermission
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_permission_id is not None:
            params["calendarPermission-id"] = calendar_permission_id
            params["calendarPermission_id"] = calendar_permission_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar()  # type:ignore
            .calendar_permissions().by_id(params.get("calendarPermission_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_update_events(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property events in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id} (PATCH).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_allowed_calendar_sharing_roles(self,
        *,
        user: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function allowedCalendarSharingRoles.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/allowedCalendarSharingRoles(User='{User}') (GET).

        Args:
            user (required): Usage: User='{User}'
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user is not None:
            params["User"] = user
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar()  # type:ignore
            .allowed_calendar_sharing_roles(_user="{_user}")().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_calendar_permissions_get_count_7010(self, *, param: str | None = None, **kwargs) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/calendarPermissions/$count (GET).

        Args:
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar()  # type:ignore
            .calendar_permissions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_calendar_view_delta(self,
        *,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/calendarView/delta() (GET).

        Args:
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().calendar_view()  # type:ignore
            .delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_create_attachments(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/attachments (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).attachments()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_create_extensions(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/extensions (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).extensions()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_delete_attachments(self,
        *,
        event_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/attachments/{attachment-id} (DELETE).

        Args:
            event_id (required): The unique identifier of event
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).attachments().by_id(params
            .get("attachment_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_delete_extensions(self,
        *,
        event_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/extensions/{extension-id} (DELETE).

        Args:
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).extensions().by_id(params
            .get("extension_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_get_attachments(self,
        *,
        event_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/attachments/{attachment-id} (GET).

        Args:
            event_id (required): The unique identifier of event
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).attachments().by_id(params
            .get("attachment_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_get_calendar(self,
        *,
        event_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendar from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/calendar (GET).

        Args:
            event_id (required): The unique identifier of event
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).calendar().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_get_count_0c79(self, *, param: str | None = None, **kwargs) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/$count (GET).

        Args:
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events().count()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_get_extensions(self,
        *,
        event_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/extensions/{extension-id} (GET).

        Args:
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).extensions().by_id(params
            .get("extension_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_list_attachments(self,
        *,
        event_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/attachments (GET).

        Args:
            event_id (required): The unique identifier of event
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_list_extensions(self,
        *,
        event_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/extensions (GET).

        Args:
            event_id (required): The unique identifier of event
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_list_instances(self,
        *,
        event_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get instances from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/instances (GET).

        Args:
            event_id (required): The unique identifier of event
            start_date_time (required): The start date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T19:00:00-08:00
            end_date_time (required): The end date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).instances().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_update_extensions(self,
        *,
        event_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/extensions/{extension-id} (PATCH).

        Args:
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).extensions().by_id(params
            .get("extension_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_attachments_get_count_53a3(self,
        *,
        event_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/attachments/$count (GET).

        Args:
            event_id (required): The unique identifier of event
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_delta(self,
        *,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/delta() (GET).

        Args:
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events().delta()()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_event_accept(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action accept.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/accept (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).accept()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_event_attachments_create_upload_session(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/attachments/createUploadSession (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_event_cancel(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action cancel.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/cancel (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).cancel()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_event_decline(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action decline.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/decline (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).decline()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_event_dismiss_reminder(self, *, event_id: str, **kwargs) -> MSGraphResponse:
        """Invoke action dismissReminder.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/dismissReminder (POST).

        Args:
            event_id (required): The unique identifier of event

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).dismiss_reminder().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_event_forward(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/forward (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).forward()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_event_instances_delta(self,
        *,
        event_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/instances/delta() (GET).

        Args:
            event_id (required): The unique identifier of event
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).instances().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_event_permanent_delete(self, *, event_id: str, **kwargs) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/permanentDelete (POST).

        Args:
            event_id (required): The unique identifier of event

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_event_snooze_reminder(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action snoozeReminder.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/snoozeReminder (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).snooze_reminder()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_event_tentatively_accept(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action tentativelyAccept.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/tentativelyAccept (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).tentatively_accept()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_events_extensions_get_count_785d(self,
        *,
        event_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/events/{event-id}/extensions/$count (GET).

        Args:
            event_id (required): The unique identifier of event
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().events()  # type:ignore
            .by_id(params.get("event_id", "")).extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_get_schedule(self, *, request_body: Mapping[str, object] | None = None, **kwargs) -> MSGraphResponse:
        """Invoke action getSchedule.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/getSchedule (POST).

        Args:
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        # No parameters
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().get_schedule()  # type:ignore
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_permanent_delete(self, **kwargs) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/calendar/permanentDelete (POST).

        Args:
            (no parameters)

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        # No parameters
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar().permanent_delete()  # type:ignore
            .post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_create_calendars(self,
        *,
        calendar_group_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create Calendar.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_delete_calendars(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property calendars for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id} (DELETE).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_get_calendars(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendars from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id} (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_get_count_9c6e(self, *, param: str | None = None, **kwargs) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/$count (GET).

        Args:
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().count()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_list_calendars(self,
        *,
        calendar_group_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List calendars.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_update_calendars(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property calendars in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id} (PATCH).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_allowed_calendar_sharing_roles(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        user: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function allowedCalendarSharingRoles.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/allowedCalendarSharingRoles(User='{User}') (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            user (required): Usage: User='{User}'
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if user is not None:
            params["User"] = user
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).allowed_calendar_sharing_roles(_user="{_user}")()
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_calendar_view_delta(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/calendarView/delta() (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).calendar_view().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_delta(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/delta() (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_event_accept(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action accept.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/accept (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .accept().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_event_attachments_create_upload_session(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/attachments/createUploadSession (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_event_cancel(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action cancel.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/cancel (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .cancel().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_event_decline(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action decline.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/decline (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .decline().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_event_dismiss_reminder(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action dismissReminder.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/dismissReminder (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .dismiss_reminder().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_event_forward(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/forward (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_event_instances_delta(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/instances/delta() (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .instances().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_event_permanent_delete(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/permanentDelete (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_event_snooze_reminder(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action snoozeReminder.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/snoozeReminder (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .snooze_reminder().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_events_event_tentatively_accept(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action tentativelyAccept.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/tentativelyAccept (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .tentatively_accept().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_get_schedule(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action getSchedule.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/getSchedule (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).get_schedule()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendar_group_calendars_calendar_permanent_delete(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/permanentDelete (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_create_calendar_permissions(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to calendarPermissions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/calendarPermissions (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).calendar_permissions()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_create_events(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to events for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_delete_calendar_permissions(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        calendar_permission_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property calendarPermissions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/calendarPermissions/{calendarPermission-id} (DELETE).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            calendar_permission_id (required): The unique identifier of calendarPermission
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if calendar_permission_id is not None:
            params["calendarPermission-id"] = calendar_permission_id
            params["calendarPermission_id"] = calendar_permission_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).calendar_permissions().by_id(params
            .get("calendarPermission_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_delete_events(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property events for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id} (DELETE).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_get_calendar_permissions(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        calendar_permission_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendarPermissions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/calendarPermissions/{calendarPermission-id} (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            calendar_permission_id (required): The unique identifier of calendarPermission
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if calendar_permission_id is not None:
            params["calendarPermission-id"] = calendar_permission_id
            params["calendarPermission_id"] = calendar_permission_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).calendar_permissions().by_id(params
            .get("calendarPermission_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_get_count_9aae(self,
        *,
        calendar_group_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/$count (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_get_events(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get events from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id} (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_list_calendar_permissions(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendarPermissions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/calendarPermissions (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).calendar_permissions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_list_calendar_view(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendarView from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/calendarView (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            start_date_time (required): The start date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T19:00:00-08:00
            end_date_time (required): The end date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).calendar_view().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_list_events(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get events from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_update_calendar_permissions(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        calendar_permission_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property calendarPermissions in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/calendarPermissions/{calendarPermission-id} (PATCH).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            calendar_permission_id (required): The unique identifier of calendarPermission
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if calendar_permission_id is not None:
            params["calendarPermission-id"] = calendar_permission_id
            params["calendarPermission_id"] = calendar_permission_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).calendar_permissions().by_id(params
            .get("calendarPermission_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_update_events(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property events in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id} (PATCH).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_calendar_permissions_get_count_3d5b(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/calendarPermissions/$count (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).calendar_permissions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_create_attachments(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/attachments (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_create_extensions(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/extensions (POST).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_delete_attachments(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/attachments/{attachment-id} (DELETE).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().by_id(params.get("attachment_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_delete_extensions(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/extensions/{extension-id} (DELETE).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().by_id(params.get("extension_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_get_attachments(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/attachments/{attachment-id} (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().by_id(params.get("attachment_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_get_calendar(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendar from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/calendar (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .calendar().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_get_count_4a49(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/$count (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_get_extensions(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/extensions/{extension-id} (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().by_id(params.get("extension_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_list_attachments(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/attachments (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_list_extensions(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/extensions (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_list_instances(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get instances from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/instances (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            start_date_time (required): The start date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T19:00:00-08:00
            end_date_time (required): The end date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .instances().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_update_extensions(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/extensions/{extension-id} (PATCH).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().by_id(params.get("extension_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_attachments_get_count_2b84(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/attachments/$count (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_groups_calendars_events_extensions_get_count_baf0(self,
        *,
        calendar_group_id: str,
        calendar_id: str,
        event_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendarGroups/{calendarGroup-id}/calendars/{calendar-id}/events/{event-id}/extensions/$count (GET).

        Args:
            calendar_group_id (required): The unique identifier of calendarGroup
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_group_id is not None:
            params["calendarGroup-id"] = calendar_group_id
            params["calendarGroup_id"] = calendar_group_id
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_groups().by_id(params  # type:ignore
            .get("calendarGroup_id", "")).calendars().by_id(params
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendar_view_delta(self,
        *,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/calendarView/delta() (GET).

        Args:
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendar_view().delta()()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_create_calendar_permissions(self,
        *,
        calendar_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to calendarPermissions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/calendarPermissions (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).calendar_permissions()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_create_events(self,
        *,
        calendar_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create event.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_delete_calendar_permissions(self,
        *,
        calendar_id: str,
        calendar_permission_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property calendarPermissions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/calendarPermissions/{calendarPermission-id} (DELETE).

        Args:
            calendar_id (required): The unique identifier of calendar
            calendar_permission_id (required): The unique identifier of calendarPermission
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if calendar_permission_id is not None:
            params["calendarPermission-id"] = calendar_permission_id
            params["calendarPermission_id"] = calendar_permission_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).calendar_permissions().by_id(params
            .get("calendarPermission_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_delete_events(self,
        *,
        calendar_id: str,
        event_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property events for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id} (DELETE).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_get_calendar_permissions(self,
        *,
        calendar_id: str,
        calendar_permission_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendarPermissions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/calendarPermissions/{calendarPermission-id} (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            calendar_permission_id (required): The unique identifier of calendarPermission
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if calendar_permission_id is not None:
            params["calendarPermission-id"] = calendar_permission_id
            params["calendarPermission_id"] = calendar_permission_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).calendar_permissions().by_id(params
            .get("calendarPermission_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_get_count_669b(self, *, param: str | None = None, **kwargs) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/$count (GET).

        Args:
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().count()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_get_events(self,
        *,
        calendar_id: str,
        event_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get events from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id} (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_list_calendar_permissions(self,
        *,
        calendar_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendarPermissions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/calendarPermissions (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).calendar_permissions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_list_calendar_view(self,
        *,
        calendar_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendarView from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/calendarView (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            start_date_time (required): The start date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T19:00:00-08:00
            end_date_time (required): The end date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).calendar_view().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_list_events(self,
        *,
        calendar_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get events from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_update_calendar_permissions(self,
        *,
        calendar_id: str,
        calendar_permission_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property calendarPermissions in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/calendarPermissions/{calendarPermission-id} (PATCH).

        Args:
            calendar_id (required): The unique identifier of calendar
            calendar_permission_id (required): The unique identifier of calendarPermission
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if calendar_permission_id is not None:
            params["calendarPermission-id"] = calendar_permission_id
            params["calendarPermission_id"] = calendar_permission_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).calendar_permissions().by_id(params
            .get("calendarPermission_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_update_events(self,
        *,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property events in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id} (PATCH).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_allowed_calendar_sharing_roles(self,
        *,
        calendar_id: str,
        user: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function allowedCalendarSharingRoles.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/allowedCalendarSharingRoles(User='{User}') (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            user (required): Usage: User='{User}'
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if user is not None:
            params["User"] = user
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).allowed_calendar_sharing_roles(_user="{_user}")()
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_calendar_view_delta(self,
        *,
        calendar_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/calendarView/delta() (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).calendar_view().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_delta(self,
        *,
        calendar_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/delta() (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_event_accept(self,
        *,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action accept.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/accept (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .accept().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_event_attachments_create_upload_session(self,
        *,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/attachments/createUploadSession (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_event_cancel(self,
        *,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action cancel.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/cancel (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .cancel().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_event_decline(self,
        *,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action decline.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/decline (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .decline().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_event_dismiss_reminder(self,
        *,
        calendar_id: str,
        event_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action dismissReminder.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/dismissReminder (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .dismiss_reminder().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_event_forward(self,
        *,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/forward (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_event_instances_delta(self,
        *,
        calendar_id: str,
        event_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/instances/delta() (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .instances().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_event_permanent_delete(self,
        *,
        calendar_id: str,
        event_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/permanentDelete (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_event_snooze_reminder(self,
        *,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action snoozeReminder.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/snoozeReminder (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .snooze_reminder().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_events_event_tentatively_accept(self,
        *,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action tentativelyAccept.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/tentativelyAccept (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .tentatively_accept().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_get_schedule(self,
        *,
        calendar_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action getSchedule.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/getSchedule (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).get_schedule()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_permanent_delete(self, *, calendar_id: str, **kwargs) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/permanentDelete (POST).

        Args:
            calendar_id (required): The unique identifier of calendar

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_calendar_permissions_get_count_8761(self,
        *,
        calendar_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/calendarPermissions/$count (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).calendar_permissions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_create_attachments(self,
        *,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/attachments (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_create_extensions(self,
        *,
        calendar_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/extensions (POST).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_delete_attachments(self,
        *,
        calendar_id: str,
        event_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/attachments/{attachment-id} (DELETE).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().by_id(params.get("attachment_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_delete_extensions(self,
        *,
        calendar_id: str,
        event_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/extensions/{extension-id} (DELETE).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().by_id(params.get("extension_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_get_attachments(self,
        *,
        calendar_id: str,
        event_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/attachments/{attachment-id} (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().by_id(params.get("attachment_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_get_calendar(self,
        *,
        calendar_id: str,
        event_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendar from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/calendar (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .calendar().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_get_count_0f8c(self,
        *,
        calendar_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/$count (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_get_extensions(self,
        *,
        calendar_id: str,
        event_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/extensions/{extension-id} (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().by_id(params.get("extension_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_list_attachments(self,
        *,
        calendar_id: str,
        event_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/attachments (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_list_extensions(self,
        *,
        calendar_id: str,
        event_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/extensions (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_list_instances(self,
        *,
        calendar_id: str,
        event_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get instances from me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/instances (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            start_date_time (required): The start date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T19:00:00-08:00
            end_date_time (required): The end date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .instances().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_update_extensions(self,
        *,
        calendar_id: str,
        event_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in me.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/extensions/{extension-id} (PATCH).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().by_id(params.get("extension_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_attachments_get_count_22f3(self,
        *,
        calendar_id: str,
        event_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/attachments/$count (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_calendars_events_extensions_get_count_b93c(self,
        *,
        calendar_id: str,
        event_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/calendars/{calendar-id}/events/{event-id}/extensions/$count (GET).

        Args:
            calendar_id (required): The unique identifier of calendar
            event_id (required): The unique identifier of event
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if calendar_id is not None:
            params["calendar-id"] = calendar_id
            params["calendar_id"] = calendar_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().calendars().by_id(params  # type:ignore
            .get("calendar_id", "")).events().by_id(params.get("event_id", ""))
            .extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_create_attachments(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Add attachment.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/attachments (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).attachments().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_create_extensions(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/extensions (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).extensions().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_delete_attachments(self,
        *,
        event_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete attachment.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/attachments/{attachment-id} (DELETE).

        Args:
            event_id (required): The unique identifier of event
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).attachments().by_id(params.get("attachment_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_delete_extensions(self,
        *,
        event_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/extensions/{extension-id} (DELETE).

        Args:
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).extensions().by_id(params.get("extension_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_get_attachments(self,
        *,
        event_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/attachments/{attachment-id} (GET).

        Args:
            event_id (required): The unique identifier of event
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).attachments().by_id(params.get("attachment_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_get_calendar(self,
        *,
        event_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendar from me.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/calendar (GET).

        Args:
            event_id (required): The unique identifier of event
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).calendar().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_get_count_ee29(self, *, param: str | None = None, **kwargs) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/events/$count (GET).

        Args:
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().count()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_get_extensions(self,
        *,
        event_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/extensions/{extension-id} (GET).

        Args:
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).extensions().by_id(params.get("extension_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_list_attachments(self,
        *,
        event_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List attachments.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/attachments (GET).

        Args:
            event_id (required): The unique identifier of event
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_list_extensions(self,
        *,
        event_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/extensions (GET).

        Args:
            event_id (required): The unique identifier of event
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_list_instances(self,
        *,
        event_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List instances.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/instances (GET).

        Args:
            event_id (required): The unique identifier of event
            start_date_time (required): The start date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T19:00:00-08:00
            end_date_time (required): The end date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).instances().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_update_extensions(self,
        *,
        event_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in me.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/extensions/{extension-id} (PATCH).

        Args:
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).extensions().by_id(params.get("extension_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_attachments_get_count_1985(self,
        *,
        event_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/attachments/$count (GET).

        Args:
            event_id (required): The unique identifier of event
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_delta(self,
        *,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/events/delta() (GET).

        Args:
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().delta()()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_event_accept(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action accept.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/accept (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).accept().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_event_attachments_create_upload_session(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/attachments/createUploadSession (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_event_cancel(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action cancel.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/cancel (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).cancel().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_event_decline(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action decline.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/decline (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).decline().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_event_dismiss_reminder(self, *, event_id: str, **kwargs) -> MSGraphResponse:
        """Invoke action dismissReminder.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/dismissReminder (POST).

        Args:
            event_id (required): The unique identifier of event

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).dismiss_reminder().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_event_forward(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/forward (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_event_instances_delta(self,
        *,
        event_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/instances/delta() (GET).

        Args:
            event_id (required): The unique identifier of event
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).instances().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_event_permanent_delete(self, *, event_id: str, **kwargs) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/permanentDelete (POST).

        Args:
            event_id (required): The unique identifier of event

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_event_snooze_reminder(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action snoozeReminder.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/snoozeReminder (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).snooze_reminder()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_event_tentatively_accept(self,
        *,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action tentativelyAccept.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/tentativelyAccept (POST).

        Args:
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).tentatively_accept()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_events_extensions_get_count_e2bd(self,
        *,
        event_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/events/{event-id}/extensions/$count (GET).

        Args:
            event_id (required): The unique identifier of event
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().events().by_id(params  # type:ignore
            .get("event_id", "")).extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_create_child_folders(self,
        *,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create mailSearchFolder.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_create_message_rules(self,
        *,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create rule.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messageRules (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).message_rules()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_create_messages(self,
        *,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create message in a mailfolder.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_delete_child_folders(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property childFolders for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1} (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_delete_message_rules(self,
        *,
        mail_folder_id: str,
        message_rule_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete messageRule.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messageRules/{messageRule-id} (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).message_rules().by_id(params
            .get("messageRule_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_delete_messages(self,
        *,
        mail_folder_id: str,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property messages for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id} (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_delete_messages_content(self,
        *,
        mail_folder_id: str,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete media content for the navigation property messages in me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/$value (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).value().delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_get_child_folders(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        include_hidden_folders: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get childFolders from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1} (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            include_hidden_folders (optional): Include Hidden Folders
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if include_hidden_folders is not None:
            params["includeHiddenFolders"] = include_hidden_folders
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_get_count_c00e(self,
        *,
        include_hidden_folders: str | None = None,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/$count (GET).

        Args:
            include_hidden_folders (optional): Include Hidden Folders
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if include_hidden_folders is not None:
            params["includeHiddenFolders"] = include_hidden_folders
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders().count()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_get_message_rules(self,
        *,
        mail_folder_id: str,
        message_rule_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get rule.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messageRules/{messageRule-id} (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).message_rules().by_id(params
            .get("messageRule_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_get_messages(self,
        *,
        mail_folder_id: str,
        message_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messages from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id} (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_get_messages_content(self,
        *,
        mail_folder_id: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """List messages.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/$value (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).value().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_list_child_folders(self,
        *,
        mail_folder_id: str,
        include_hidden_folders: str | None = None,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List childFolders.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            include_hidden_folders (optional): Include Hidden Folders
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if include_hidden_folders is not None:
            params["includeHiddenFolders"] = include_hidden_folders
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_list_message_rules(self,
        *,
        mail_folder_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List rules.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messageRules (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).message_rules().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_list_messages(self,
        *,
        mail_folder_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List messages.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_update_child_folders(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property childFolders in me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1} (PATCH).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_update_message_rules(self,
        *,
        mail_folder_id: str,
        message_rule_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update rule.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messageRules/{messageRule-id} (PATCH).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).message_rules().by_id(params
            .get("messageRule_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_update_messages(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property messages in me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id} (PATCH).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_update_messages_content(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update media content for the navigation property messages in me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/$value (PUT).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).value().put(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_create_message_rules(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to messageRules for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_create_messages(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to messages for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_delete_message_rules(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_rule_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property messageRules for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules/{messageRule-id} (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules().by_id(params
            .get("messageRule_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_delete_messages(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property messages for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id} (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_delete_messages_content(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete media content for the navigation property messages in me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/$value (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .value().delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_get_count_25bc(self,
        *,
        mail_folder_id: str,
        include_hidden_folders: str | None = None,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/$count (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            include_hidden_folders (optional): Include Hidden Folders
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if include_hidden_folders is not None:
            params["includeHiddenFolders"] = include_hidden_folders
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().count()
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_get_message_rules(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_rule_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messageRules from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules/{messageRule-id} (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules().by_id(params
            .get("messageRule_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_get_messages(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messages from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id} (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_get_messages_content(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Get media content for the navigation property messages from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/$value (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .value().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_list_message_rules(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messageRules from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_list_messages(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messages from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_update_message_rules(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_rule_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property messageRules in me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules/{messageRule-id} (PATCH).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules().by_id(params
            .get("messageRule_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_update_messages(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property messages in me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id} (PATCH).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_update_messages_content(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update media content for the navigation property messages in me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/$value (PUT).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .value().put(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_message_rules_get_count_6a96(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules/$count (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_create_attachments(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_create_extensions(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_delete_attachments(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments/{attachment-id} (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().by_id(params.get("attachment_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_delete_extensions(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions/{extension-id} (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().by_id(params.get("extension_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_get_attachments(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments/{attachment-id} (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().by_id(params.get("attachment_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_get_count_56da(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/$count (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_get_extensions(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions/{extension-id} (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().by_id(params.get("extension_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_list_attachments(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_list_extensions(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_update_extensions(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions/{extension-id} (PATCH).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().by_id(params.get("extension_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_attachments_get_count_65b2(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments/$count (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_child_folders_messages_extensions_get_count_cf94(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions/$count (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_delta(self,
        *,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/delta() (GET).

        Args:
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders().delta()()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_delta(self,
        *,
        mail_folder_id: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/delta() (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().delta()()
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_copy(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action copy.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/copy (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).copy().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_delta(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        change_type: str | None = None,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/delta() (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            change_type (optional): A custom query option to filter the delta response based on the type of change. Supported values are created, updated or deleted.
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if change_type is not None:
            params["changeType"] = change_type
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_attachments_create_upload_session(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments/createUploadSession (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_copy(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action copy.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/copy (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .copy().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_create_forward(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createForward.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/createForward (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .create_forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_create_reply(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReply.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/createReply (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .create_reply().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_create_reply_all(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReplyAll.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/createReplyAll (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .create_reply_all().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_forward(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/forward (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_move(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action move.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/move (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .move().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_permanent_delete(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/permanentDelete (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_reply(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action reply.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/reply (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .reply().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_reply_all(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action replyAll.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/replyAll (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .reply_all().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_messages_message_send(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action send.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/send (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .send().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_move(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action move.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/move (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).move().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_child_folders_mail_folder_permanent_delete(self,
        *,
        mail_folder_id: str,
        mail_folder_id1: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/permanentDelete (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_copy(self,
        *,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action copy.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/copy (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).copy()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_delta(self,
        *,
        mail_folder_id: str,
        change_type: str | None = None,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/delta() (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            change_type (optional): A custom query option to filter the delta response based on the type of change. Supported values are created, updated or deleted.
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if change_type is not None:
            params["changeType"] = change_type
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().delta()()
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_attachments_create_upload_session(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/attachments/createUploadSession (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_copy(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action copy.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/copy (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).copy().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_create_forward(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createForward.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/createForward (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).create_forward()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_create_reply(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReply.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/createReply (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).create_reply()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_create_reply_all(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReplyAll.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/createReplyAll (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).create_reply_all()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_forward(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/forward (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_move(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action move.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/move (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).move().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_permanent_delete(self,
        *,
        mail_folder_id: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/permanentDelete (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_reply(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action reply.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/reply (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).reply().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_reply_all(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action replyAll.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/replyAll (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).reply_all().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_messages_message_send(self,
        *,
        mail_folder_id: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action send.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/send (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).send().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_move(self,
        *,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action move.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/move (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).move()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_mail_folder_permanent_delete(self, *, mail_folder_id: str, **kwargs) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/permanentDelete (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).permanent_delete()
            .post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_message_rules_get_count_3d24(self,
        *,
        mail_folder_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messageRules/$count (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).message_rules().count()
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_create_attachments(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/attachments (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).attachments()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_create_extensions(self,
        *,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/extensions (POST).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).extensions()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_delete_attachments(self,
        *,
        mail_folder_id: str,
        message_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/attachments/{attachment-id} (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).attachments().by_id(params
            .get("attachment_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_delete_extensions(self,
        *,
        mail_folder_id: str,
        message_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property extensions for me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/extensions/{extension-id} (DELETE).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).extensions().by_id(params.get("extension_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_get_attachments(self,
        *,
        mail_folder_id: str,
        message_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/attachments/{attachment-id} (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).attachments().by_id(params
            .get("attachment_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_get_count_363a(self,
        *,
        mail_folder_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/$count (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().count()
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_get_extensions(self,
        *,
        mail_folder_id: str,
        message_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/extensions/{extension-id} (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).extensions().by_id(params.get("extension_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_list_attachments(self,
        *,
        mail_folder_id: str,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/attachments (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_list_extensions(self,
        *,
        mail_folder_id: str,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/extensions (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_update_extensions(self,
        *,
        mail_folder_id: str,
        message_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in me.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/extensions/{extension-id} (PATCH).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).extensions().by_id(params.get("extension_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_attachments_get_count_16fa(self,
        *,
        mail_folder_id: str,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/attachments/$count (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_mail_folders_messages_extensions_get_count_8860(self,
        *,
        mail_folder_id: str,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/mailFolders/{mailFolder-id}/messages/{message-id}/extensions/$count (GET).

        Args:
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().mail_folders()  # type:ignore
            .by_id(params.get("mailFolder_id", "")).messages().by_id(params
            .get("message_id", "")).extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_create_attachments(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Add attachment.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/attachments (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).attachments()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_create_extensions(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create open extension.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/extensions (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).extensions()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_delete_attachments(self,
        *,
        message_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property attachments for me.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/attachments/{attachment-id} (DELETE).

        Args:
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).attachments().by_id(params
            .get("attachment_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_delete_extensions(self,
        *,
        message_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete open extension.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/extensions/{extension-id} (DELETE).

        Args:
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).extensions().by_id(params.get("extension_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_get_attachments(self,
        *,
        message_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachment.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/attachments/{attachment-id} (GET).

        Args:
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).attachments().by_id(params
            .get("attachment_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_get_count_a82e(self,
        *,
        include_hidden_messages: str | None = None,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/messages/$count (GET).

        Args:
            include_hidden_messages (optional): Include Hidden Messages
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if include_hidden_messages is not None:
            params["includeHiddenMessages"] = include_hidden_messages
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().count()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_get_extensions(self,
        *,
        message_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get open extension.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/extensions/{extension-id} (GET).

        Args:
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).extensions().by_id(params.get("extension_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_list_attachments(self,
        *,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """List attachments.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/attachments (GET).

        Args:
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_list_extensions(self,
        *,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get open extension.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/extensions (GET).

        Args:
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_update_extensions(self,
        *,
        message_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in me.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/extensions/{extension-id} (PATCH).

        Args:
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).extensions().by_id(params.get("extension_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_attachments_get_count_2181(self,
        *,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/attachments/$count (GET).

        Args:
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_delta(self,
        *,
        change_type: str | None = None,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /me/messages/delta() (GET).

        Args:
            change_type (optional): A custom query option to filter the delta response based on the type of change. Supported values are created, updated or deleted.
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if change_type is not None:
            params["changeType"] = change_type
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().delta()()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_extensions_get_count_d960(self,
        *,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/extensions/$count (GET).

        Args:
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_attachments_create_upload_session(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/attachments/createUploadSession (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_copy(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action copy.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/copy (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).copy().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_create_forward(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createForward.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/createForward (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).create_forward()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_create_reply(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReply.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/createReply (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).create_reply()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_create_reply_all(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReplyAll.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/createReplyAll (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).create_reply_all()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_forward(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/forward (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_move(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action move.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/move (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).move().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_permanent_delete(self, *, message_id: str, **kwargs) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/permanentDelete (POST).

        Args:
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_reply(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action reply.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/reply (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).reply().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_reply_all(self,
        *,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action replyAll.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/replyAll (POST).

        Args:
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).reply_all().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def me_messages_message_send(self, *, message_id: str, **kwargs) -> MSGraphResponse:
        """Invoke action send.

        Microsoft Outlook Mail and Calendar API method: /me/messages/{message-id}/send (POST).

        Args:
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().messages().by_id(params  # type:ignore
            .get("message_id", "")).send().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_create_events(self,
        *,
        user_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to events for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events (POST).

        Args:
            user_id (required): The unique identifier of user
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar()  # type:ignore
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_create_mail_folders(self,
        *,
        user_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to mailFolders for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders (POST).

        Args:
            user_id (required): The unique identifier of user
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders()  # type:ignore
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_create_messages(self,
        *,
        user_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to messages for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages (POST).

        Args:
            user_id (required): The unique identifier of user
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders()  # type:ignore
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_delete_events(self,
        *,
        user_id: str,
        event_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property events for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_delete_mail_folders(self,
        *,
        user_id: str,
        mail_folder_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property mailFolders for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_delete_messages(self,
        *,
        user_id: str,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property messages for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_delete_messages_content(self,
        *,
        user_id: str,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete media content for the navigation property messages in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/$value (DELETE).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).value().delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_get_events(self,
        *,
        user_id: str,
        event_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get events from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_get_mail_folders(self,
        *,
        user_id: str,
        mail_folder_id: str,
        include_hidden_folders: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get mailFolders from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            include_hidden_folders (optional): Include Hidden Folders
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if include_hidden_folders is not None:
            params["includeHiddenFolders"] = include_hidden_folders
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_get_messages(self,
        *,
        user_id: str,
        message_id: str,
        include_hidden_messages: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messages from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            include_hidden_messages (optional): Include Hidden Messages
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if include_hidden_messages is not None:
            params["includeHiddenMessages"] = include_hidden_messages
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_get_messages_content(self,
        *,
        user_id: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Get media content for the navigation property messages from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/$value (GET).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).value().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_list_events(self,
        *,
        user_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get events from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events (GET).

        Args:
            user_id (required): The unique identifier of user
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().get(params=params)  # type:ignore
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_list_mail_folders(self,
        *,
        user_id: str,
        include_hidden_folders: str | None = None,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get mailFolders from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders (GET).

        Args:
            user_id (required): The unique identifier of user
            include_hidden_folders (optional): Include Hidden Folders
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if include_hidden_folders is not None:
            params["includeHiddenFolders"] = include_hidden_folders
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().get(params=params)  # type:ignore
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_list_messages(self,
        *,
        user_id: str,
        include_hidden_messages: str | None = None,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messages from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages (GET).

        Args:
            user_id (required): The unique identifier of user
            include_hidden_messages (optional): Include Hidden Messages
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if include_hidden_messages is not None:
            params["includeHiddenMessages"] = include_hidden_messages
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().get(params=params)  # type:ignore
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_update_events(self,
        *,
        user_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property events in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_update_mail_folders(self,
        *,
        user_id: str,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property mailFolders in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_update_messages(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property messages in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_update_messages_content(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update media content for the navigation property messages in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/$value (PUT).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).value().put(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_create_attachments(self,
        *,
        user_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to attachments for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/attachments (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).attachments().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_create_extensions(self,
        *,
        user_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to extensions for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/extensions (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).extensions().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_delete_attachments(self,
        *,
        user_id: str,
        event_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property attachments for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/attachments/{attachment-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).attachments().by_id(params.get("attachment_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_delete_extensions(self,
        *,
        user_id: str,
        event_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property extensions for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/extensions/{extension-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).extensions().by_id(params.get("extension_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_get_attachments(self,
        *,
        user_id: str,
        event_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/attachments/{attachment-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).attachments().by_id(params.get("attachment_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_get_calendar(self,
        *,
        user_id: str,
        event_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get calendar from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/calendar (GET).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).calendar().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_get_count_d443(self,
        *,
        user_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().count().get(params=params)  # type:ignore
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_get_extensions(self,
        *,
        user_id: str,
        event_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/extensions/{extension-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).extensions().by_id(params.get("extension_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_list_attachments(self,
        *,
        user_id: str,
        event_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/attachments (GET).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_list_extensions(self,
        *,
        user_id: str,
        event_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/extensions (GET).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_list_instances(self,
        *,
        user_id: str,
        event_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get instances from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/instances (GET).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            start_date_time (required): The start date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T19:00:00-08:00
            end_date_time (required): The end date and time of the time range, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).instances().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_update_extensions(self,
        *,
        user_id: str,
        event_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/extensions/{extension-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).extensions().by_id(params.get("extension_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_attachments_get_count_711f(self,
        *,
        user_id: str,
        event_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/attachments/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_events_extensions_get_count_0041(self,
        *,
        user_id: str,
        event_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/extensions/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_create_child_folders(self,
        *,
        user_id: str,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to childFolders for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_create_message_rules(self,
        *,
        user_id: str,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to messageRules for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messageRules (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).message_rules()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_create_messages(self,
        *,
        user_id: str,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to messages for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_delete_child_folders(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property childFolders for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_delete_message_rules(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_rule_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property messageRules for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messageRules/{messageRule-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).message_rules().by_id(params
            .get("messageRule_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_delete_messages(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property messages for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_delete_messages_content(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete media content for the navigation property messages in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/$value (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .value().delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_get_child_folders(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        include_hidden_folders: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get childFolders from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1} (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            include_hidden_folders (optional): Include Hidden Folders
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if include_hidden_folders is not None:
            params["includeHiddenFolders"] = include_hidden_folders
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_get_count_7dc9(self,
        *,
        user_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().count().get(params=params)  # type:ignore
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_get_message_rules(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_rule_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messageRules from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messageRules/{messageRule-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).message_rules().by_id(params
            .get("messageRule_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_get_messages(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messages from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_get_messages_content(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Get media content for the navigation property messages from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/$value (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .value().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_list_child_folders(self,
        *,
        user_id: str,
        mail_folder_id: str,
        include_hidden_folders: str | None = None,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get childFolders from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            include_hidden_folders (optional): Include Hidden Folders
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if include_hidden_folders is not None:
            params["includeHiddenFolders"] = include_hidden_folders
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_list_message_rules(self,
        *,
        user_id: str,
        mail_folder_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messageRules from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messageRules (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).message_rules().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_list_messages(self,
        *,
        user_id: str,
        mail_folder_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messages from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_update_child_folders(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property childFolders in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_update_message_rules(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_rule_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property messageRules in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messageRules/{messageRule-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).message_rules().by_id(params
            .get("messageRule_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_update_messages(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property messages in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_update_messages_content(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update media content for the navigation property messages in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/$value (PUT).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .value().put(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_create_message_rules(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to messageRules for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_create_messages(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to messages for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_delete_message_rules(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_rule_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property messageRules for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules/{messageRule-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules().by_id(params
            .get("messageRule_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_delete_messages(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property messages for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_delete_messages_content(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete media content for the navigation property messages in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/$value (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .value().delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_get_count_d2d8(self,
        *,
        user_id: str,
        mail_folder_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_get_message_rules(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_rule_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messageRules from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules/{messageRule-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules().by_id(params
            .get("messageRule_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_get_messages(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messages from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_get_messages_content(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Get media content for the navigation property messages from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/$value (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .value().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_list_message_rules(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messageRules from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_list_messages(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get messages from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_update_message_rules(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_rule_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property messageRules in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules/{messageRule-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_rule_id (required): The unique identifier of messageRule
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_rule_id is not None:
            params["messageRule-id"] = message_rule_id
            params["messageRule_id"] = message_rule_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules().by_id(params
            .get("messageRule_id", "")).patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_update_messages(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property messages in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_update_messages_content(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update media content for the navigation property messages in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/$value (PUT).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .value().put(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_message_rules_get_count_22e6(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messageRules/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).message_rules().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_create_attachments(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to attachments for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_create_extensions(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to extensions for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_delete_attachments(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property attachments for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments/{attachment-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().by_id(params.get("attachment_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_delete_extensions(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property extensions for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions/{extension-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().by_id(params.get("extension_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_get_attachments(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments/{attachment-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().by_id(params.get("attachment_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_get_count_a576(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_get_extensions(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions/{extension-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().by_id(params.get("extension_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_list_attachments(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_list_extensions(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_update_extensions(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions/{extension-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().by_id(params.get("extension_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_attachments_get_count_5ef0(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_child_folders_messages_extensions_get_count_1433(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/extensions/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_message_rules_get_count_f330(self,
        *,
        user_id: str,
        mail_folder_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messageRules/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).message_rules().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_create_attachments(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to attachments for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/attachments (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .attachments().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_create_extensions(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to extensions for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/extensions (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .extensions().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_delete_attachments(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property attachments for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/attachments/{attachment-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .attachments().by_id(params.get("attachment_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_delete_extensions(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property extensions for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/extensions/{extension-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .extensions().by_id(params.get("extension_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_get_attachments(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/attachments/{attachment-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .attachments().by_id(params.get("attachment_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_get_count_9534(self,
        *,
        user_id: str,
        mail_folder_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_get_extensions(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/extensions/{extension-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .extensions().by_id(params.get("extension_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_list_attachments(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/attachments (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_list_extensions(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/extensions (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_update_extensions(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/extensions/{extension-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .extensions().by_id(params.get("extension_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_attachments_get_count_3c73(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/attachments/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_mail_folders_messages_extensions_get_count_651d(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/extensions/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_create_attachments(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to attachments for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/attachments (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).attachments()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_create_extensions(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Create new navigation property to extensions for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/extensions (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).extensions()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_delete_attachments(self,
        *,
        user_id: str,
        message_id: str,
        attachment_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property attachments for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/attachments/{attachment-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).attachments().by_id(params
            .get("attachment_id", "")).delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_delete_extensions(self,
        *,
        user_id: str,
        message_id: str,
        extension_id: str,
        if_match: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Delete navigation property extensions for users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/extensions/{extension-id} (DELETE).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            if_match (optional): ETag

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if if_match is not None:
            params["If-Match"] = if_match
            params["If_Match"] = if_match
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).extensions().by_id(params.get("extension_id", ""))
            .delete(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_get_attachments(self,
        *,
        user_id: str,
        message_id: str,
        attachment_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/attachments/{attachment-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            attachment_id (required): The unique identifier of attachment
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if attachment_id is not None:
            params["attachment-id"] = attachment_id
            params["attachment_id"] = attachment_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).attachments().by_id(params
            .get("attachment_id", "")).get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_get_count_0f52(self,
        *,
        user_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().count().get(params=params)  # type:ignore
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_get_extensions(self,
        *,
        user_id: str,
        message_id: str,
        extension_id: str,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/extensions/{extension-id} (GET).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).extensions().by_id(params.get("extension_id", ""))
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_list_attachments(self,
        *,
        user_id: str,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get attachments from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/attachments (GET).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).attachments().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_list_extensions(self,
        *,
        user_id: str,
        message_id: str,
        param: str | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_select: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get extensions from users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/extensions (GET).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            param (optional):
            dollar_orderby (optional): Order items by property values
            dollar_select (optional): Select properties to be returned
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).extensions().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_update_extensions(self,
        *,
        user_id: str,
        message_id: str,
        extension_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Update the navigation property extensions in users.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/extensions/{extension-id} (PATCH).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            extension_id (required): The unique identifier of extension
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if extension_id is not None:
            params["extension-id"] = extension_id
            params["extension_id"] = extension_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).extensions().by_id(params.get("extension_id", ""))
            .patch(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_attachments_get_count_a586(self,
        *,
        user_id: str,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/attachments/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).attachments().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_messages_extensions_get_count_5628(self,
        *,
        user_id: str,
        message_id: str,
        param: str | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Get the number of the resource.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/extensions/$count (GET).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            param (optional):

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if param is not None:
            params["param"] = param
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).extensions().count().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_delta(self,
        *,
        user_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/delta() (GET).

        Args:
            user_id (required): The unique identifier of user
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().delta()().get(params=params)  # type:ignore
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_event_accept(self,
        *,
        user_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action accept.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/accept (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).accept().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_event_attachments_create_upload_session(self,
        *,
        user_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/attachments/createUploadSession (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_event_cancel(self,
        *,
        user_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action cancel.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/cancel (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).cancel().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_event_decline(self,
        *,
        user_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action decline.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/decline (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).decline().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_event_dismiss_reminder(self,
        *,
        user_id: str,
        event_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action dismissReminder.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/dismissReminder (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).dismiss_reminder().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_event_forward(self,
        *,
        user_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/forward (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_event_instances_delta(self,
        *,
        user_id: str,
        event_id: str,
        start_date_time: str,
        end_date_time: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/instances/delta() (GET).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            start_date_time (required): The start date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            end_date_time (required): The end date and time of the time range in the function, represented in ISO 8601 format. For example, 2019-11-08T20:00:00-08:00
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if start_date_time is not None:
            params["startDateTime"] = start_date_time
        if end_date_time is not None:
            params["endDateTime"] = end_date_time
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).instances().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_event_permanent_delete(self,
        *,
        user_id: str,
        event_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/permanentDelete (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_event_snooze_reminder(self,
        *,
        user_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action snoozeReminder.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/snoozeReminder (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).snooze_reminder()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_events_event_tentatively_accept(self,
        *,
        user_id: str,
        event_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action tentativelyAccept.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/events/{event-id}/tentativelyAccept (POST).

        Args:
            user_id (required): The unique identifier of user
            event_id (required): The unique identifier of event
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if event_id is not None:
            params["event-id"] = event_id
            params["event_id"] = event_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().calendar().by_id(params  # type:ignore
            .get("event_id", "")).tentatively_accept()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_delta(self,
        *,
        user_id: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/delta() (GET).

        Args:
            user_id (required): The unique identifier of user
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().delta()()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_delta(self,
        *,
        user_id: str,
        mail_folder_id: str,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/delta() (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_copy(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action copy.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/copy (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).copy().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_delta(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        change_type: str | None = None,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/delta() (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            change_type (optional): A custom query option to filter the delta response based on the type of change. Supported values are created, updated or deleted.
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if change_type is not None:
            params["changeType"] = change_type
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_attachments_create_upload_session(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/attachments/createUploadSession (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_copy(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action copy.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/copy (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .copy().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_create_forward(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createForward.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/createForward (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .create_forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_create_reply(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReply.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/createReply (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .create_reply().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_create_reply_all(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReplyAll.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/createReplyAll (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .create_reply_all().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_forward(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/forward (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_move(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action move.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/move (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .move().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_permanent_delete(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/permanentDelete (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_reply(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action reply.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/reply (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .reply().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_reply_all(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action replyAll.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/replyAll (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .reply_all().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_messages_message_send(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action send.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/messages/{message-id}/send (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).messages().by_id(params.get("message_id", ""))
            .send().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_move(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action move.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/move (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).move().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_child_folders_mail_folder_permanent_delete(self,
        *,
        user_id: str,
        mail_folder_id: str,
        mail_folder_id1: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/childFolders/{mailFolder-id1}/permanentDelete (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            mail_folder_id1 (required): The unique identifier of mailFolder

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if mail_folder_id1 is not None:
            params["mailFolder-id1"] = mail_folder_id1
            params["mailFolder_id1"] = mail_folder_id1
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).child_folders().by_id(params
            .get("mailFolder_id1", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_copy(self,
        *,
        user_id: str,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action copy.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/copy (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).copy().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_delta(self,
        *,
        user_id: str,
        mail_folder_id: str,
        change_type: str | None = None,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/delta() (GET).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            change_type (optional): A custom query option to filter the delta response based on the type of change. Supported values are created, updated or deleted.
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if change_type is not None:
            params["changeType"] = change_type
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().delta()().get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_attachments_create_upload_session(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/attachments/createUploadSession (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_copy(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action copy.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/copy (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .copy().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_create_forward(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createForward.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/createForward (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .create_forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_create_reply(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReply.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/createReply (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .create_reply().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_create_reply_all(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReplyAll.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/createReplyAll (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .create_reply_all().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_forward(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/forward (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_move(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action move.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/move (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .move().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_permanent_delete(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/permanentDelete (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_reply(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action reply.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/reply (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .reply().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_reply_all(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action replyAll.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/replyAll (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .reply_all().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_messages_message_send(self,
        *,
        user_id: str,
        mail_folder_id: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action send.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/messages/{message-id}/send (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).messages().by_id(params.get("message_id", ""))
            .send().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_move(self,
        *,
        user_id: str,
        mail_folder_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action move.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/move (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).move().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_mail_folders_mail_folder_permanent_delete(self,
        *,
        user_id: str,
        mail_folder_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/mailFolders/{mailFolder-id}/permanentDelete (POST).

        Args:
            user_id (required): The unique identifier of user
            mail_folder_id (required): The unique identifier of mailFolder

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if mail_folder_id is not None:
            params["mailFolder-id"] = mail_folder_id
            params["mailFolder_id"] = mail_folder_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("mailFolder_id", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_delta(self,
        *,
        user_id: str,
        change_type: str | None = None,
        param: str | None = None,
        dollar_select: list[str] | None = None,
        dollar_orderby: list[str] | None = None,
        dollar_expand: list[str] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke function delta.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/delta() (GET).

        Args:
            user_id (required): The unique identifier of user
            change_type (optional): A custom query option to filter the delta response based on the type of change. Supported values are created, updated or deleted.
            param (optional):
            dollar_select (optional): Select properties to be returned
            dollar_orderby (optional): Order items by property values
            dollar_expand (optional): Expand related entities

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if change_type is not None:
            params["changeType"] = change_type
        if param is not None:
            params["param"] = param
        if dollar_select is not None:
            _val = ",".join(dollar_select) if isinstance(dollar_select, list) else str(dollar_select)
            params["$select"] = _val
            params["dollar_select"] = _val
        if dollar_orderby is not None:
            _val = ",".join(dollar_orderby) if isinstance(dollar_orderby, list) else str(dollar_orderby)
            params["$orderby"] = _val
            params["dollar_orderby"] = _val
        if dollar_expand is not None:
            _val = ",".join(dollar_expand) if isinstance(dollar_expand, list) else str(dollar_expand)
            params["$expand"] = _val
            params["dollar_expand"] = _val
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().delta()()  # type:ignore
            .get(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_attachments_create_upload_session(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createUploadSession.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/attachments/createUploadSession (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).attachments().create_upload_session()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_copy(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action copy.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/copy (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).copy().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_create_forward(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createForward.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/createForward (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).create_forward()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_create_reply(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReply.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/createReply (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).create_reply()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_create_reply_all(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action createReplyAll.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/createReplyAll (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).create_reply_all()
            .post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_forward(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action forward.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/forward (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).forward().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_move(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action move.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/move (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).move().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_permanent_delete(self,
        *,
        user_id: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action permanentDelete.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/permanentDelete (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).permanent_delete().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_reply(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action reply.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/reply (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).reply().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_reply_all(self,
        *,
        user_id: str,
        message_id: str,
        request_body: Mapping[str, object] | None = None,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action replyAll.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/replyAll (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message
            request_body (optional): Request body data

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).reply_all().post(request_body or {}, params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )

    def users_user_messages_message_send(self,
        *,
        user_id: str,
        message_id: str,
        **kwargs,
    ) -> MSGraphResponse:
        """Invoke action send.

        Microsoft Outlook Mail and Calendar API method: /users/{user-id}/messages/{message-id}/send (POST).

        Args:
            user_id (required): The unique identifier of user
            message_id (required): The unique identifier of message

        Returns:
            MSGraphResponse: Standardized response wrapper

        """
        params: dict[str, object] = {}
        if user_id is not None:
            params["user-id"] = user_id
            params["user_id"] = user_id
        if message_id is not None:
            params["message-id"] = message_id
            params["message_id"] = message_id
        if kwargs:
            for k, v in kwargs.items():
                if v is None:
                    continue
                # CSV-encode OData list params; mirror hyphenated -> snake
                if k in ("$select", "$expand") and isinstance(v, list):
                    _vv = ",".join(v)
                    params[k] = _vv
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = _vv
                else:
                    params[k] = v
                    _mk = self._sanitize_py_name(k)
                    if _mk != k:
                        params[_mk] = v

        try:
            response = (
            self.client.me().mail_folders().by_id(params  # type:ignore
            .get("message_id", "")).send().post(params=params)
            )
            return self._handle_response(response)
        except Exception as e:
            return MSGraphResponse(
                success=False,
                error=f"Microsoft Outlook Mail and Calendar API API call failed: {e!s}",
            )
